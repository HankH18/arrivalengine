"""Arrival Engine — arrival digests for Arena Hall.

Deliberately empty of imports. A heavy package ``__init__`` turns one broken submodule
into a total collection failure, so import the module you need directly::

    from arrival.contracts import Dossier
    from arrival.util import slug
"""

__version__ = "0.1.0"
