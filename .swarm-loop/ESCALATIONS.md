# ESCALATIONS — arrival-engine

The loop writes here when it hits something frozen (or otherwise locked) that it
believes is **wrong**. A freeze is a locked door, not a wall: the loop's obligation is
to bring the problem to you rather than work around it, silently re-freeze it, or keep
scoring against a goal it thinks is broken.

**Nothing in this file has been changed by the loop. Escalating is asking. It is never
granting.** Every entry is a request awaiting your decision. Only you can grant one:
`freeze --amend "<reason>"` is the only mechanism that alters a frozen goal or test,
and `escalate --resolve <ID>` records what you decided.

<sub>Mirror of `.swarm-loop/escalations.jsonl` (append-only — that file is the source of truth). Regenerated by
every `escalate`; editing this file by hand changes nothing.</sub>

## OPEN (0)

_None. Nothing is waiting on you._

## RESOLVED (7)

### ESC-007 · frozen-test · `.swarm-loop/acceptance/test_t3_extractor.py:595-603` — resolved 2026-09-04T02:54:57

- filed 2026-09-04T02:29:43 (cycle 5)
- **what was wrong**: The frozen suite cannot detect an R11 privacy leak through hub evidence, and two independent lanes have now measured it. FIX-EXTRACT2 ran eight sabotages against extract.py, including S8 — the full pre-repair shape in which CandidateHub.doc_id is trusted uncorroborated and a whole document's facts are bulk-attached to a hub. That is a live path to displaying withheld material: research._supported_hubs (research.py:461-508), whose entire job is dropping a hub whose evidence was taste-excluded, is satisfied by BORROWED evidence from a different document. ALL EIGHT SABOTAGES, S8 INCLUDED, LEFT THE FROZEN GATE AT 100.0 / t3=10 / t6=12. I verified the cause by reading the file rather than taking the lane's word: test_t3_extractor.py:595-603 asserts only that Hub.evidence_fact_ids are non-dangling and that they span both source documents. Nothing anywhere in .swarm-loop/acceptance/ asserts that an evidence fact has anything to DO with the hub it evidences — grep for evidence_fact_ids across the frozen suite returns contract-shape checks, an alias table, a fixture builder and those two assertions. So the property whose absence reopens R11 is ungraded. The code is fixed and merged, so nothing is broken right now; what is missing is the gate that would catch it coming back. R11 is this product's core promise and the one thing a demo cannot survive getting wrong.
- **proposed**: Amend .swarm-loop/acceptance/test_t3_extractor.py to add one criterion, proposed verbatim by the lane that found the gap: EVERY Hub.evidence_fact_id must resolve to a fact whose SOURCE DOCUMENT mentions the hub's label. It is mechanical, fixture-independent, needs no new fixture data, and the lane measured that it would have caught all of S1/S2/S3/S8. This requires 'freeze --amend', which is yours alone — I am asking, not proposing to do it. If you would rather not move a frozen byte this late, the alternative is to accept that this property is covered only by the project's own tests (tests/extract/test_t034_hub_doc_id_corroboration.py, which I merged and which does grade it) and not by the scored gate.
- **decision**: AUTHORIZED by the owner 2026-09-04, verbatim: 'for esc-007, Use your best judgment. Give my authorization.' Standing grant to amend the frozen T-3 suite with the proposed criterion, at my discretion. I am NOT exercising it yet: a re-verification is in flight because the sabotage recipe that produced the evidence for this escalation was found to be unsound in the not-caught direction (pytest's own pythonpath=['src'] wins over PYTHONPATH, so a sabotaged copy can silently measure the worktree). If the re-measurement CONFIRMS the gap I will run freeze --amend under this authorization and record it. If it REFUTES the gap, the amendment is unnecessary and I will record that instead — the authorization will go unused rather than be spent on a number I could not vouch for.

### ESC-006 · frozen-test · `.swarm-loop/acceptance/test_t6_pipeline.py — three sabotages the t6 suite survives` — resolved 2026-09-04T00:24:06

- filed 2026-09-04T00:08:25 (cycle 3)
- **what was wrong**: The T-6 lane measured three mutations that leave criteria_t6 at 11/11 GREEN, each verified by sabotage rather than suspected. NONE is a wrong assertion — every frozen t6 assertion is correct and the implementation passes all of them. Each is a MISSING assertion that lets a materially broken pipeline score full marks. (1) THE WORST: the suite cannot detect a pipeline that NEVER APPLIES TASTE. Replacing 'facts = await apply_taste(...)' with 'facts = candidates' leaves t6 11/11 green. The assertions are 'at least one fact survives' and 'facts_kept + facts_excluded == len(facts)', and both hold identically when taste is never called — everything is kept and excluded is 0. Taste is R11-R14, the 'seen vs dossiered' line this product is scored on, and T-6's own acceptance says '>=1 excluded fact' — but no frozen test asserts an exclusion, because the frozen t6 corpus contains no sentence the taste rules exclude (every sentence returns 'keep' from taste.rule_verdict, verified). (2) It cannot detect CONNECTOR-ORDER TRUNCATION: with docs_per_connector=2 and max_docs_total=4 over three connectors, taking the first four in connector order satisfies every assertion while the third connector never reaches the model at all. Going wide is the entire retrieval strategy. (3) It cannot detect a MISSING PER-CONNECTOR TRIM: max_docs_total=4 masks it, because 4 across 3 sources is <=2 each however much each source returned. CONTAINMENT, which I verified myself before proposing an amendment — the lesson from ESC-004: test_t6_pipeline.py's corpus is INTERNAL to that module. Nothing else references its _raw_doc helper and it does not read the shared fixtures/dossiers/ corpus that t5, t7 and t8 pin, so amending it moves no other module's target. T-8 is the only lane in flight and does not touch it.
- **proposed**: Amend test_t6_pipeline.py, three additions, no assertion changed and no shared fixture touched. (1) Append one deterministically-excluded sentence to _raw_doc's text — the lane measured f'{name} bought a four-bedroom house on Pecan Street for 2.4 million dollars.' returns exclude/home_or_property from taste.rule_verdict with NO LLM call, so it costs the harness nothing — and assert any(fact.excluded for fact in dossier.facts) in test_a_resolved_dossier_holds_facts_and_hubs_and_its_report_row_accounts_for_them. (2) In test_docs_per_connector_and_max_docs_total_are_both_respected, assert the tight selection spans at least two of the three connectors. (3) Add a case with docs_per_connector=2 and max_docs_total=40 so the per-connector trim is the only thing capping, and assert <=2 from each source there. I recommend taking all three: (1) especially, because the product's headline differentiator can currently be absent entirely with the metric green, and this is the last chance before the audit loops start scoring against it.
- **decision**: All three taken, granted by Hank 2026-09-04 (delegated decision). Landed as freeze amendments #2 and #3. Each new assertion verified NON-VACUOUS by an independent agent re-running the sabotage cold with the marker confirmed present in the loaded module: taste removed -> the new test fails; flat connector-order truncation -> the spread assertion fails naming github as never asked; per-connector trim deleted -> the new trim test fails. My first draft of the spread assertion was itself vacuous and was caught before sealing. Targets re-derived from the amended suite: acceptance_collected 120 -> 122, criteria_t6 10 -> 12.

### ESC-004 · frozen-test · `.swarm-loop/acceptance/fixtures/dossiers/ — recency and type-boost coverage` — resolved 2026-09-03T23:25:06

- filed 2026-09-03T22:56:15 (cycle 1)
- **what was wrong**: The rung-2 verifier proved two of criteria_t5's scored criteria are structurally undiscriminating, because of what the FROZEN corpus contains rather than because of any product defect. (1) Every hub in all five frozen dossiers has recency 1.0, so min(ra,rb) and max(ra,rb) are the same function on every input the frozen suite supplies. Inverting the recency rule leaves criteria_t5 at 8/8. Verified two ways: sabotage (t5 unchanged at 8) and a witness that fired 95 times printing exactly one distinct line, ra=1.0 rb=1.0. (2) Only 2 of the 10 TYPE_BOOST values are observable — investor and topic. city:austin and topic:remote-work are carried by all five people so their idf clamps to 0 and multiplies the boost away; company and school hubs are unshared. A wrong boost for any of board, cause, city, company, event, person, school or technology is invisible to the scored metric. MITIGATING, and why this is not urgent: the product itself is NOT unprotected — tests/graph/test_t5_scoring.py::test_contribution_recency_is_the_min_of_the_two_edges does catch the inversion. So this is a weakness in what the SCORED number can see, not a live product hole.
- **proposed**: Either (a) amend the frozen corpus with one additional dossier pair sharing a hub at unequal recencies, plus one shared hub of a type other than investor/topic, so both properties become observable to criteria_t5 — this is a freeze --amend and therefore yours; or (b) accept the limitation deliberately and record it, on the grounds that the ticket's own suite covers the behaviour. I lean (b) for now: the corpus was built and approved for taste and matching discrimination, the amendment would move a hash mid-run, and the gap is covered elsewhere. Not blocking; the build proceeds either way.
- **decision**: Option (b), decided by the orchestrator on the owner's delegation, 2026-09-03. NOT passive acceptance — the deciding fact is one I did not have when I filed it: the corpus is pinned by THREE frozen modules, not one. grep shows test_t5_matching.py, test_t7_digest.py and test_t8_web.py all pin values derived from these dossiers (scores 100/67, IDF 0.5108). T-7 is being built against those numbers RIGHT NOW and T-8 is queued behind it, so amending a recency would move a pinned target under a lane mid-flight — precisely the goalpost shift the freeze exists to prevent, and a worse failure than the blindness it would cure. The blindness is also bounded: the verifier independently recomputed IDF, type boosts, min-recency, REF and normalisation against both corpora and found ZERO mismatches, so the implementation is correct today; the exposure is future regression, not present wrongness. Compensating action taken instead of nothing: ticket minted to close the same gap in the NON-frozen layer, where no goalpost moves — tests/graph/ gains explicit coverage for the min-vs-max recency rule and for the eight type boosts the frozen corpus cannot observe. Option (a) remains available and cheap once T-7 and T-8 have landed; if we want it then, it is one amendment plus the pinned scores that follow.

### ESC-005 · frozen-test · `.swarm-loop/acceptance/test_t2_resolver.py:515-521 — test_conflicting_evidence_verdict_is_what_keeps_a_namesake_doc_out` — resolved 2026-09-03T23:20:16

- filed 2026-09-03T23:13:15 (cycle 2)
- **what was wrong**: A frozen assertion and DESIGN's own words pull in opposite directions, and only you can pick. THE ASSERTION, verbatim (lines 515-521): it takes the decoy-deceased-namesake case, flips every 'no' verdict to 'yes' leaving documents, evidence and confidences untouched, and then requires those documents to be ACCEPTED — 'assert doc_id in accepted_when_not_vetoed'. The three flipped verdicts carry evidence reading 'Employer: Ostlerhaus Institute. Work location: Halifax, Nova Scotia.' for a person whose details are 'Staff researcher at Harrowgate Systems' and 'Austin'. So the frozen suite requires a document that explicitly names a different employer AND a different city to be accepted, purely because the model attached 'yes' to it. WHY IT IS IN TENSION INDEPENDENT OF ANY IMPLEMENTATION: DESIGN.md:235 says 'negative evidence hard-rejects... a single contradiction must veto', which is a statement about the EVIDENCE, not about which token the model emitted. As a sabotage companion the assertion is doing legitimate work — it proves the rejection above it was caused by the negative verdict rather than by something incidental about those documents — but it does that by flipping the ONE dimension that also carries the contradiction, so it cannot distinguish 'polarity caused the rejection' from 'the contradiction caused it'. MEASURED, not argued: extending the veto to yes verdicts turns this scored frozen criterion red, so the tension is real and not a matter of taste. I dispatched a lane to make contradicting-yes documents rejected; that goal is unreachable while this stands, and the lane correctly did not implement it. WHAT WAS SHIPPED ANYWAY, and it is the more serious half: an accepted contradicting document could previously hand over a STRONG KEY — a durable identifier for a different human being — and now cannot.
- **proposed**: Option A, my recommendation, keeps the assertion's sabotage value and unblocks the goal: re-script the three decoy verdicts as 'yes' on the DISAMBIGUATOR dimension instead of the match dimension — a non-conflicting verbatim span from the same documents, e.g. yes on 'role'. Documents, confidences and source kinds stay identical, so it still proves the earlier rejection was not incidental, while no longer requiring a self-contradicting 'yes' to be accepted. This is a freeze --amend on one frozen test and therefore yours. Option B: leave it exactly as written, and we read 'negative evidence hard-rejects' as strictly polarity-gated — the model is expected to emit 'no' and the veto acts on it. Then T-2b's premise is retired, what shipped is the whole of what is available, and nothing further is owed. Both are defensible; B is free and A costs one amendment. NOT BLOCKING either way: the build proceeds, T-2 is at target, and the strong-key hole is already closed.
- **decision**: Option A, granted by Hank 2026-09-03. The frozen sabotage companion now flips the DISAMBIGUATOR rather than the match: each decoy verdict becomes a 'yes' on 'role' carrying a different verbatim span from the same document, verified to assert no employer and no work location. Landed as freeze amendment #1. Verified 14/14 t2, verify green, and the amended test proven NON-VACUOUS by a resolver sabotaged to drop those three doc ids — it fails, with the sabotage confirmed present in the loaded source. The design rule 'negative evidence hard-rejects' is now implementable without contradicting a frozen assertion.

### ESC-002 · other · `SPEC-Q2-tavily-account` — resolved 2026-09-03T20:54:22

- filed 2026-09-03T14:21:10 (cycle 0)
- **what was wrong**: SPEC Q2 resolves to 'create the Tavily account' by default, and RESEARCH.md records that every alternative free search tier died in 2025-2026 (Brave no-card removed Feb 2026, Google CSE closed to new signups Jan 2026, Bing retired Aug 2025). Tavily needs a human signup. Without it the search connector falls back to DuckDuckGo-lite only and, per SPEC Q2, coverage drops. This does NOT block T-1: C7 forbids network in tests, so the connector is built and graded against recorded fixtures either way. It only bites at the T-9 live build.
- **proposed**: Hank creates the free no-card Tavily account before the T-9 live build and puts TAVILY_API_KEY in .env (gitignored). If he would rather not, say so and the run records DDG-lite-only coverage as the shipped behaviour — the code path is built and tested regardless.
- **decision**: Deferred by Hank, 2026-09-03: not providing the Tavily key now. Proceed without it. C7 forbids network in tests so T-1's search connector is built and graded against recorded fixtures regardless; the key is needed only at the T-9 live build, and DDG-lite is the fallback for that one slot. Revisit before the T-9 run.

### ESC-001 · blocked-ticket · `T-9` — resolved 2026-09-03T20:54:21

- filed 2026-09-03T14:21:10 (cycle 0)
- **what was wrong**: T-9's acceptance is four HUMAN gates the swarm structurally cannot close: (1) 'python -m arrival build' against the ten REAL people in data/roster.yaml needs live network plus ANTHROPIC_API_KEY and TAVILY_API_KEY, none of which are in this environment (verified: env holds none of the four keys .env.example declares); (2) TASKS.md T-9.1 requires every displayed fact to be OPENED AT ITS URL and confirmed by a person, and explicitly forbids hand-editing the JSON, so no agent can substitute for that review; (3) the Render deploy (S7) needs an account and a card-free signup a human performs; (4) S8 requires the deploy URL to be LIVE at submission. Confirmed with the user at Phase 0: build every code artifact, stop at the gates.
- **proposed**: Swarm builds T-9's code artifacts only: render.yaml, tests/test_t9_committed_dossiers.py (which SKIPS with an explicit reason while data/dossiers/ is empty, so it is a real gate once the human build has run), and the README with hours log, exclusion policy, Q1-Q5 answers and the next-month paragraph. Hank then: creates the Tavily account, exports ANTHROPIC_API_KEY + TAVILY_API_KEY + GITHUB_TOKEN + CONTACT_EMAIL, runs 'python -m arrival build --roster data/roster.yaml --out data/dossiers', hand-reviews each dossier against its source URLs, commits data/dossiers + data/docs, and deploys to Render. T-9 carries NO frozen metric so a permanently-0 human-gated goal cannot trip the goal-stall kill switch.
- **decision**: Confirmed by Hank, 2026-09-03: swarm builds T-9's code artifacts only (render.yaml, the committed-dossier validator, the README with hours log / exclusion policy / Q1-Q5 / next-month paragraph) and stops at the four human gates. This was already agreed at Phase 0; re-asking it was the loop's error, not a real open question.

### ESC-003 · other · `.swarm-loop/acceptance/fixtures/taste_cases_frozen.yaml` — resolved 2026-09-03T20:54:21

- filed 2026-09-03T14:21:10 (cycle 0)
- **what was wrong**: SPEC S3 and DESIGN's verification strategy both require the taste fixture to be EXTERNAL ground truth approved by the owner, not the filter's own table — it is the scored differentiator and the product 'dies on the wrong side of' this line (R11). TASKS.md T-4.1 wants the approval recorded as '# approved_by: <name> <date>'. INTAKE DIVERGENCE: the doc set puts the graded fixture at tests/fixtures/taste_cases.yaml, which is inside T-4's OWN scope — a filter graded against a fixture its own author can write is not being graded (this exact attack is a recorded failure mode). So the GRADING copy is the orchestrator-owned frozen one named above; T-4 still ships its own fixture for its own unit tests and for the approval artifact.
- **proposed**: Hank reviews the frozen taste cases at the plan checkpoint — in particular the tricky keeps ('raised a Series B', 'board of a children's hospital foundation', 'their company settled a trademark dispute', 'spoke about burnout recovery on a podcast') and the OWNER REVIEW NOTES block at the end of the file — and either approves as written or names the cases to re-rule. Approval is recorded in the freeze and in the file's first line.
- **decision**: Approved as written by Hank, 2026-09-03. All seven owner-review rulings stand, including the four debatable ones (burnout-podcast KEEP, trademark-settled KEEP, city-council-testimony KEEP, six-month-absence EXCLUDE via fail-closed). Held-back minimal pairs and their inherited rulings stand with them. Hank: 'worst case we can tweak it later' — a later change is a freeze --amend, his call, not the loop's.
